Django==5.2.14
djangorestframework==3.17.1
django-cors-headers==4.9.0
mysqlclient==2.2.8
llama-index-core==0.14.24
llama-index-embeddings-ollama==0.10.0
ollama==0.6.2
pymilvus==3.0.1
# Windows 必须显式安装：pymilvus 的 milvus-lite extra 带 sys_platform != "win32" 标记，
# `pip install "pymilvus[milvus-lite]"` 在 Windows 上不会带上它。
milvus-lite==3.2.1
PyMuPDF==1.28.2
python-docx>=1.1.2,<2.0
# 仅 Docker(Linux) 环境使用；Windows 上可安装但不可运行。
gunicorn==23.0.0
